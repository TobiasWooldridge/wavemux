name: Docs

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  markdown-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Markdown links
        run: python3 scripts/check-md-links.py --all
